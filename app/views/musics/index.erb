<h1>Home Page</h1>

<% @musics.each do |music| %>
  <div class="music">
    <h3><a href="<%= music.url %>"><%= music.title %></a> <small>(by: <%= music.author %>)</small></h3>
  </div>
<% end %>

<a href="/musics/new">Post a song... It's FREE (for now)!</a>